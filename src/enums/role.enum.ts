export enum Role {
  SuperAdmin = 'superadmin',
  Admin = 'admin',
  Doctor = 'doctor',
  Client = 'client',
}

export enum DoctorStatus {
  Accepted = 'accepted',
  Rejected = 'rejected',
  Pending = 'pending',
}
